services:
  litellm:
    # Pinned: v1.81.13+ maps/strips anthropic-beta headers per provider, which is
    # what stops Bedrock returning "invalid beta flag" for Claude Code traffic.
    # main-stable can lag behind that fix.
    image: docker.litellm.ai/berriai/litellm:v1.97.0
    #########################################
    ## config.yaml + the Akto hook, mounted into the container ##
    volumes:
      - ./config.yaml:/app/config.yaml
      - ./custom_hooks.py:/app/custom_hooks.py
    command:
      - "--config=/app/config.yaml"
    ##############################################
    ports:
      - "4000:4000" # Map the container port to the host, change the host port if necessary
    environment:
      DATABASE_URL: "postgresql://llmproxy:dbpassword9090@db:5432/litellm"
      STORE_MODEL_IN_DB: "True" # allows adding models to proxy via UI
    env_file:
      - .env # Load local .env file
    depends_on:
      - db  # Indicates that this service depends on the 'db' service, ensuring 'db' starts first
    healthcheck:  # Defines the health check configuration for the container
      test:
        - CMD-SHELL
        - python3 -c "import urllib.request; urllib.request.urlopen('http://localhost:4000/health/liveliness')"  # Command to execute for health check
      interval: 30s  # Perform health check every 30 seconds
      timeout: 10s   # Health check command times out after 10 seconds
      retries: 3     # Retry up to 3 times if health check fails
      start_period: 40s  # Wait 40 seconds after container start before beginning health checks


  # Claude Code -> this -> litellm. Removes the beta flags Bedrock rejects.
  strip-proxy:
    image: python:3.12-slim
    working_dir: /app
    command: python -u strip_proxy.py
    environment:
      UPSTREAM_HOST: litellm
      UPSTREAM_PORT: "4000"
      PORT: "4001"
    volumes:
      - ./strip_proxy.py:/app/strip_proxy.py:ro
    ports:
      - "4001:4001"
    depends_on:
      - litellm

  db:
    image: postgres:16
    restart: always
    container_name: litellm_db
    environment:
      POSTGRES_DB: litellm
      POSTGRES_USER: llmproxy
      POSTGRES_PASSWORD: dbpassword9090
    # No published port: litellm reaches postgres over the compose network as
    # host "db". Publishing 5432 would expose it with the hardcoded password.
    volumes:
      - postgres_data:/var/lib/postgresql/data # Persists Postgres data across container restarts
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -d litellm -U llmproxy"]
      interval: 1s
      timeout: 5s
      retries: 10

volumes:
  postgres_data:
    name: litellm_postgres_data # Named volume for Postgres data persistence
